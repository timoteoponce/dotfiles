# Global gitignore (git core.excludesFile -> ~/.gitignore).
# Keep this conservative — it applies to every repo. Build artifacts and
# language-specific ignores belong in each project's own .gitignore.

# macOS
.DS_Store
.AppleDouble
.LSOverride
._*
.Spotlight-V100
.Trashes
Icon?

# Linux
.fuse_hidden*
.directory
.Trash-*
.nfs*

# Editors / IDEs
*.swp
*.swo
*~
.idea/
.vscode/
.netrwhist
.nvim.lua
.exrc

# Tooling
.direnv/
.env
.env.local
node_modules/
__pycache__/
*.log

# Claude
.claude/settings.local.json
