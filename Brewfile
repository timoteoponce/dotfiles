# Bootstrap layer only. Everything with a clean standalone binary lives in
# mise.toml instead (see README). Run: brew bundle --file Brewfile

brew "fish"   # login shell: must exist before chsh / /etc/shells
brew "git"
brew "gh"     # GitHub auth + git credential helper
brew "mise"   # drives the tool + runtime layer (mise.toml)
brew "tmux"

cask "ghostty"
cask "font-jetbrains-mono-nerd-font"

# Docker runtime is intentionally not managed here.
# Install Docker Desktop, OrbStack, or: brew install colima docker docker-compose
